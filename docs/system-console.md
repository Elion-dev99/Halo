# システムコンソール（隠しメニュー）

わかりやすい手順は **`docs/SETUP_GUIDE.md` の「2. システム開発・管理者用メニュー」** を参照してください。

要点だけ:

1. `VITE_PLATFORM_ADMIN_EMAILS` または Firestore `platformAdmins/{UID}` で管理者登録  
2. ログイン後、サイドバーの **H ロゴを 5 回クリック** → `/sys`  
3. 一般ユーザーにはメニューも反応も出ません  
